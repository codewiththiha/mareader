[package]
name = "library-wasm"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
reader-ui = { package = "mareader", path = "../..", default-features = false, features = ["library"] }
