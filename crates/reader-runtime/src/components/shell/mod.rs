//! The reader's chrome: the rail family (the shared aside container, the two
//! mount points, the panels) and the reader-only document titles. The layout
//! truth itself (`ShellController`) is shared chrome — `app-ui`.

pub mod sidebar;
pub mod titlebar;
