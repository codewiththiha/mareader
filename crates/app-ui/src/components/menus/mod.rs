//! App menus, built on the floating-system primitives (MenuPopover wrapper).
//! The reader's own menu is NOT here: it lives in
//! `reader-runtime::components::menus`, beside the settings modal it opens.

pub mod appearance_menu;
